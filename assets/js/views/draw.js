/**
* View: draw
*/
( function( $, window, document, utilities ) {

	var draw = Gillie.View.extend({

	});

	// Global Exposition
	window.App.Views.draw = new draw();



})( this.jQuery, this, this.document, this.App.Helpers.Utilities );

