/**
* Model: draw
*/
( function( $, window, document, utilities ) {

	var draw = Gillie.Model.extend({

			// Called on instantiation
			initialize: function() {

			}

			// Default attributes
		,	defaults: {

			}

	});	

	// Global Exposition
	window.App.Models.draw = new draw();



})( this.jQuery, this, this.document, this.App.Helpers.Utilities );

